# =================================================================================
# FILE: .gitignore
# =================================================================================
# This file specifies intentionally untracked files that Git should ignore.
# It's crucial for keeping the repository clean and secure.
# =================================================================================

# Dependencies
# This directory contains all the third-party packages installed by npm.
# It should never be committed as it can be very large and is easily regenerated
# by running `npm install`.
node_modules/

# Build output directories
# These folders contain the compiled, production-ready code generated by Vite.
# They are always regenerated during the build process.
dist/
build/

# Environment variables
# These files contain sensitive information like API keys and should NEVER be
# committed to a public repository for security reasons.
.env
.env.local
.env.*.local

# System files
# macOS-specific system file that should not be tracked.
.DS_Store

# IDE / Editor settings
# This directory contains user-specific settings for Visual Studio Code.
# It should be ignored to avoid conflicts between different developers' setups.
.vscode/

# Debug & log files
# These files are generated during debugging sessions and contain temporary data.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Firebase
# Firebase-specific cache and log files.
firebase-debug.log
firebase-debug.*.log
.firebase/
.firebaserc
firestore.indexes.json
firestore.rules
storage.rules

# Vite-specific cache and temporary files
.cache/
.parcel-cache/

# Yarn Plug'n'Play files
.pnp/
.pnp.js

# Optional: OS-specific files
# Windows-specific thumbnail cache file.
Thumbs.db
